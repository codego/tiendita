# Contact form destination until Curadario has its own inbox.
CONTACT_TO=joacoditoma@gmail.com

# Public origin for Open Graph / WhatsApp previews (absolute image URLs).
# Local: http://localhost:3000 — set this to the real host (e.g. https://curadario.app) when you share.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
